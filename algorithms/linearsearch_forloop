# linear search (for loop)
def linear_search(A, target):
  for i in range(len(A)):
    if A[i] == target: # found
      return i
  return -1 # not found

# main
data = [1, 3, 18, 45, 99]
print(linear_search(data, 18)) # found
print(linear_search(data, 100)) # not found
